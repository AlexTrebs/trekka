# Google Drive Configuration (only needed if API_SOURCE=drive)
GOOGLE_FOLDER_ID=your-google-drive-folder-id
GOOGLE_API_KEY=your-google-api-key

# Admin Credentials
ADMIN_USER=
ADMIN_PASS=

# JWT Secret for session tokens
# Generate a secure random string (e.g., openssl rand -base64 32)
# IMPORTANT: Keep this secret! Do NOT commit to git
JWT_SECRET=your-jwt-secret-here

# API Source Configuration
# Options: "drive" (Google Drive API) or "trekka-api" (Firebase-based API)
API_SOURCE=drive

# Trekka API Configuration (only needed if API_SOURCE=trekka-api)
# URL of your trekka-api instance (e.g., https://your-api.vercel.app)
TREKKA_API_URL=http://localhost:8080

# Trekka API Key (REQUIRED for trekka-api)
# Get this from your Trekka API deployment
# IMPORTANT: Keep this secret! Do NOT commit to git
TREKKA_API_KEY=your-api-key-here
